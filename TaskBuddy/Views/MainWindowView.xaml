<UserControl
  x:Class="TaskBuddy.Views.MainWindowView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:converters="clr-namespace:TaskBuddy.Converters"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:TaskBuddy.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewmodels="clr-namespace:TaskBuddy.ViewModels"
  d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">

  <Grid>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <WrapPanel Margin="0,0,0,10">
      <Button
        Margin="0,0,5,0"
        Padding="5"
        Command="{Binding NavigateToCommand}"
        CommandParameter="{Binding TaskIndexViewModel}"
        Content="Index" />
      <Button
        Padding="5"
        Command="{Binding NavigateToCommand}"
        CommandParameter="{Binding TaskFormViewModel}"
        Content="Form" />
    </WrapPanel>

    <TextBlock
      Grid.Row="1"
      FontSize="20"
      Text="{Binding CurrentViewModel.Title}" />

    <ContentControl Grid.Row="2" Content="{Binding CurrentViewModel}" />

  </Grid>
</UserControl>
